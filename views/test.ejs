<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Personel Takip Sistemi</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
<div class="max-w-6xl mx-auto py-10 px-4">
    <header class="mb-8 text-center">
        <h1 class="text-4xl font-bold text-blue-600">Personel Takip Sistemi</h1>
    </header>

    <div class="flex justify-between items-center mb-6">
        <h2 class="text-xl font-semibold">Personel Listesi</h2>
        <a href="/personel/yeni" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded">
            + Yeni Personel Ekle
        </a>
    </div>

    <div class="overflow-x-auto shadow rounded bg-white">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-100">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Ad Soyad</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Departman</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Pozisyon</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">İşe Giriş</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">İşlem</th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
            <!-- Örnek veri satırı (backend'den dinamik gelecek) -->
            <% data.forEach((user) => { %>
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap"><%= user.name %></td>
                    <td class="px-6 py-4 whitespace-nowrap"><%= user.position%></td>
                    <td class="px-6 py-4 whitespace-nowrap">Yazılım</td>
                    <td class="px-6 py-4 whitespace-nowrap"><%= user.employment %></td>
                    <td class="px-6 py-4 whitespace-nowrap text-right space-x-2">
                        <a href="/personel/1" class="text-blue-500 hover:underline">Görüntüle</a>
                        <a href="/personel/1/duzenle" class="text-yellow-500 hover:underline">Düzenle</a>
                        <a href="/personel/1/sil" class="text-red-500 hover:underline">Sil</a>
                    </td>
                </tr>
            <% }) %>


            <!-- Diğer personeller burada listelenecek -->
            </tbody>
        </table>
    </div>
</div>
</body>
</html>